# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# App Pages
GET     /                             controllers.Application.index
GET     /dashboard/:xauthtoken        controllers.Application.dashboard(xauthtoken: String)

# Auth
POST    /authenticate                 controllers.Application.authenticate
GET     /logout/:xauthtoken           controllers.Application.logout(xauthtoken: String)
GET     /jumpstart/:xauthtoken        controllers.Application.jumpstart(xauthtoken: String)

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file                 controllers.Assets.at(path="/public", file)
